<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="generator" content="Preacher 2">
        <title>{{ headline|default(source.baseName) }} - Preacher</title>
{% if content matches '/<pre><code/' %}
        <link href="https://zeroconfig.github.io/Preacher/{{ asset('css/prism.css') }}" rel="stylesheet" type="text/css" />
{% endif %}
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" crossorigin="anonymous">
        <link href="https://zeroconfig.github.io/Preacher/{{ asset('css/style.css') }}" rel="stylesheet" type="text/css" />
        <link rel="canonical" href="https://zeroconfig.github.io/Preacher/{{ output.path }}" />
    </head>

    <body class="page-{{ source.path|replace({'.md': '', '/': '-'}) }}">
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <a
                    id="edit"
                    class="btn btn-primary pull-right"
                    href="https://github.com/ZeroConfig/Preacher/edit/master/{{ source.path }}"
                    target="_blank">
                    <span class="glyphicon glyphicon-edit"></span>
                    Edit this page
                </a>
                <div class="navbar-header">
                    <a class="navbar-brand" href="https://zeroconfig.github.io/Preacher/">
                        <span class="glyphicon glyphicon-console"></span>
                        &nbsp;Preacher
                    </a>
                </div>
            </div>
        </nav>

        <div class="container">
            <main>{% block content %}{{ content }}{% endblock %}</main>
        </div>

        <footer class="container text-muted">
            <p>
                Last updated:
                <a
                    href="https://github.com/ZeroConfig/Preacher/commit/{{ source.metaData.version.short }}"
                    target="_blank">{{ source.metaData.dateUpdated|date('M d Y H:i') }}</a>
                - {{ source.metaData.author.name }}
            </p>
        </footer>
{% if content matches '/<pre><code/' %}

        <script src="https://zeroconfig.github.io/Preacher/{{ asset('js/prism.js') }}" type="text/javascript"></script>
{% endif %}
    </body>
</html>

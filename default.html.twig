<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ headline|default(source.baseName) }} - Preacher</title>
{% if content matches '/<pre><code/' %}
        <link href="https://zeroconfig.github.io/Preacher/css/prism.css" rel="stylesheet" type="text/css" />
{% endif %}
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" crossorigin="anonymous">
        <link href="https://zeroconfig.github.io/Preacher/css/style.css" rel="stylesheet" type="text/css" />
        <link rel="canonical" href="https://zeroconfig.github.io/Preacher/{{ output.path }}" />
    </head>

    <body>
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <a
                    id="edit"
                    class="btn btn-primary pull-right"
                    href="https://github.com/ZeroConfig/Preacher/edit/master/{{ source.path }}"
                    target="_blank">
                    <span class="glyphicon glyphicon-edit"></span>
                    Edit this page
                </a>
                <div class="navbar-header">
                    <a class="navbar-brand" href="https://zeroconfig.github.io/Preacher/">Preacher</a>
                </div>
            </div>
        </nav>

        <div class="container">
            <main>{% block content %}{{ content }}{% endblock %}</main>
        </div>

        <footer class="container text-muted">
            <p>
                Last updated:
                {{ source.metaData.dateUpdated|date('M d Y H:i') }}
                - {{ source.metaData.author.name }}
            </p>
        </footer>
{% if content matches '/<pre><code/' %}

        <script src="https://zeroconfig.github.io/Preacher/js/prism.js" type="text/javascript"></script>
{% endif %}
    </body>
</html>
